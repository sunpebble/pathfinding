---
phase: 08-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false

must_haves:
  truths:
    - 'Each platform crawler runs without errors'
    - 'Extracted data contains all 6 core fields'
    - 'Image URLs are accessible and high-resolution'
    - 'Session persistence works for authenticated platforms'
  artifacts:
    - path: '.planning/phases/08-verification/VERIFICATION-REPORT.md'
      provides: 'Final verification results for all 5 platforms'
  key_links:
    - from: 'crawlers/*.ts'
      to: 'VERIFICATION-REPORT.md'
      via: 'manual test execution and inspection'
---

<objective>
Verify all 5 platform crawlers extract complete data with 6 core fields.

Purpose: Final acceptance testing to confirm all crawler fixes work correctly before project completion.
Output: VERIFICATION-REPORT.md documenting test results for each platform.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/ai-service/src/lib/crawlers/ctrip.ts
@apps/ai-service/src/lib/crawlers/qunar.ts
@apps/ai-service/src/lib/crawlers/mafengwo.ts
@apps/ai-service/src/lib/crawlers/tongcheng.ts
@apps/ai-service/src/lib/crawlers/xiaohongshu.ts
@apps/ai-service/src/lib/login-helper.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Ctrip and Qunar (no auth required)</name>
  <what-built>
    Ctrip and Qunar crawlers with enhanced parsing utilities:
    - parseChineseNumber() for engagement metrics
    - transformToHighRes() for image URLs
    - extractPublishDate() for dates
    - extractCtripStats()/extractQunarStats() for metrics
  </what-built>
  <how-to-verify>
    1. Run Ctrip crawler: Test with a city like "杭州" or "北京"
    2. Check extracted data for 10+ guides:
       - content: Full article text (not truncated/placeholder)
       - images: High-res URLs (containing quality params like _W_0_0_Q100)
       - author: Name present (avatar best-effort)
       - publishTime: Valid date string
       - stats: Views/likes/saves (at least views for Ctrip)
    3. Run Qunar crawler: Same city test
    4. Check same 6 fields are populated
    5. Verify image URLs by opening 2-3 in browser (should load high-res images)
  </how-to-verify>
  <resume-signal>
    Report results:
    - "Ctrip: X/10 complete records, issues: [...]"
    - "Qunar: X/10 complete records, issues: [...]"
    Or type "both-pass" if all tests pass
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Mafengwo and Tongcheng (auth required)</name>
  <what-built>
    Mafengwo and Tongcheng crawlers with:
    - Detail page navigation (fetchGuideUrls -> fetchGuideDetail pattern)
    - extractMafengwoStats()/extractTongchengStats()
    - extractMafengwoAuthor()/extractTongchengAuthor()
    - transformToHighResMfw()/transformToHighResTc()
    - Captcha/login wall detection
  </what-built>
  <how-to-verify>
    1. Ensure authenticated sessions exist:
       - Run login-helper if needed: `npx tsx apps/ai-service/src/lib/login-helper.ts mafengwo`
       - Run login-helper if needed: `npx tsx apps/ai-service/src/lib/login-helper.ts tongcheng`
    2. Run Mafengwo crawler with a city
    3. Check extracted data:
       - content: Full article from detail page (not placeholder like "XXX旅游攻略")
       - images: High-res URLs
       - author: Name and avatar
       - publishTime: Valid date
       - stats: Engagement metrics
    4. Run Tongcheng crawler with same city
    5. Check same 6 fields populated
    6. If captcha appears, note which platform and what type
  </how-to-verify>
  <resume-signal>
    Report results:
    - "Mafengwo: X/10 complete records, issues: [...]"
    - "Tongcheng: X/10 complete records, issues: [...]"
    Or type "both-pass" if all tests pass
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Xiaohongshu (auth required, includes video)</name>
  <what-built>
    Xiaohongshu crawler with:
    - detectPlaceholderContent() and isContentQualityAcceptable()
    - calculateXhsQualityScore() for content quality filtering
    - handleSessionRefresh() for auto-refresh on degradation
    - fetchNoteDetail() for video notes with fresh URLs
    - videoUrlCapturedAt timestamp for CDN expiry tracking
  </what-built>
  <how-to-verify>
    1. Ensure authenticated session:
       - Run login-helper if needed: `npx tsx apps/ai-service/src/lib/login-helper.ts xiaohongshu`
    2. Run Xiaohongshu crawler with a travel-related search (e.g., "杭州旅游")
    3. Check extracted data for 10+ notes:
       - content: Full note text (not placeholder)
       - images: High-res URLs with dimension info (imageWidth/imageHeight)
       - videos: For video notes, check videoUrl present with videoUrlCapturedAt
       - author: Name and avatar URL
       - stats: likes, saves, comments counts
    4. Note: publishTime will be empty (XHS-05 N/A - API limitation)
    5. Verify at least 1-2 video notes have video URLs
    6. Check quality filtering: Low-quality/placeholder notes should be filtered out
  </how-to-verify>
  <resume-signal>
    Report results:
    - "Xiaohongshu: X/10 complete records"
    - "Video notes found: X with valid videoUrl"
    - "Quality filtering working: yes/no"
    Or type "pass" if all tests pass
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify Session Persistence</name>
  <what-built>
    Session management module at crawlers/session/:
    - Session validators for each platform
    - Session manager with save/load/verify
    - Integration with login-helper.ts
  </what-built>
  <how-to-verify>
    1. For each authenticated platform (Mafengwo, Tongcheng, Xiaohongshu):
       a. Note the session file modification time in session storage
       b. Run the crawler once (uses existing session)
       c. Wait 1-2 minutes
       d. Run the crawler again
       e. Confirm no re-login required (session file not regenerated)
    2. Verify session files exist at expected location
    3. Confirm crawlers detect valid sessions and skip login prompts
  </how-to-verify>
  <resume-signal>
    Report results:
    - "Session persistence: Mafengwo [pass/fail], Tongcheng [pass/fail], Xiaohongshu [pass/fail]"
    Or type "all-pass" if all sessions persist correctly
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Create Verification Report</name>
  <files>.planning/phases/08-verification/VERIFICATION-REPORT.md</files>
  <action>
    Based on the checkpoint results from Tasks 1-4, create a comprehensive verification report:

    1. Summary table showing pass/fail for each platform and each requirement
    2. Per-platform sections with:
       - Records tested count
       - Field completeness breakdown
       - Sample data snippets (redacted if needed)
       - Any issues encountered
    3. Session persistence results
    4. Overall project status (ready for production / needs fixes)

    Template structure:
    ```markdown
    # Verification Report: Crawler Data Quality Fix

    ## Summary

    | Platform | VERIFY-01 (10+ records) | VERIFY-02 (6 fields) | VERIFY-03 (images) | VERIFY-04 (sessions) |
    |----------|-------------------------|----------------------|--------------------|----------------------|
    | Ctrip    | [result]                | [result]             | [result]           | N/A                  |
    | Qunar    | [result]                | [result]             | [result]           | N/A                  |
    | Mafengwo | [result]                | [result]             | [result]           | [result]             |
    | Tongcheng| [result]                | [result]             | [result]           | [result]             |
    | Xiaohongshu| [result]              | [result]             | [result]           | [result]             |

    ## Platform Details

    ### Ctrip
    [Details...]

    ### Qunar
    [Details...]

    ### Mafengwo
    [Details...]

    ### Tongcheng
    [Details...]

    ### Xiaohongshu
    [Details...]

    ## Conclusion

    [Overall assessment and any remaining issues]
    ```

  </action>
  <verify>VERIFICATION-REPORT.md exists and contains results for all 5 platforms</verify>
  <done>Comprehensive verification report created documenting test results</done>
</task>

</tasks>

<verification>
- All 5 platforms tested with 10+ records each
- All 6 core fields verified for each platform
- Image URLs confirmed accessible and high-resolution
- Session persistence verified for authenticated platforms
- VERIFICATION-REPORT.md created with complete results
</verification>

<success_criteria>

1. VERIFY-01: Each platform crawls 10+ complete records - documented in report
2. VERIFY-02: All 6 core fields have values (video only for XHS) - verified per platform
3. VERIFY-03: Image URLs accessible and high-resolution - spot-checked
4. VERIFY-04: Sessions persist across runs - tested for Mafengwo, Tongcheng, Xiaohongshu
   </success_criteria>

<output>
After completion, create `.planning/phases/08-verification/08-01-SUMMARY.md`
</output>
