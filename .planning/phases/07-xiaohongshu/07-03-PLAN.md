---
phase: 07-xiaohongshu
plan: 03
type: execute
wave: 3
depends_on: ['07-02']
files_modified:
  - apps/ai-service/src/lib/crawlers/xiaohongshu.ts
autonomous: false

must_haves:
  truths:
    - 'Crawler extracts notes with full content'
    - 'Video URLs are captured for video notes'
    - 'Image URLs include dimensions'
    - 'Author name and avatar are extracted'
    - 'Engagement metrics (likes, saves, comments) are captured'
    - 'Statistics report shows extraction quality'
  artifacts:
    - path: '.planning/phases/07-xiaohongshu/07-03-SUMMARY.md'
      provides: 'Verification evidence with extraction statistics'
  key_links:
    - from: 'crawlXiaohongshu()'
      to: 'CrawlResult'
      via: 'complete field extraction'
      pattern: 'imageUrls.*videoUrls.*likesCount'
---

<objective>
Verify Xiaohongshu crawler extracts all 6 core fields with quality filtering active

Purpose: Confirm the enhanced crawler correctly extracts full content, images with dimensions, videos (when present), author info, engagement metrics, and applies quality filtering. Verify login session support works.

Output: Verification evidence in SUMMARY.md with extraction statistics and field completeness.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-xiaohongshu/07-CONTEXT.md
@.planning/phases/07-xiaohongshu/07-02-SUMMARY.md
@apps/ai-service/src/lib/crawlers/xiaohongshu.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verification test script</name>
  <files>apps/ai-service/src/lib/crawlers/xiaohongshu.ts</files>
  <action>
Create a temporary verification script or use existing test infrastructure to:

1. Run the crawler with a test city (e.g., "北京")
2. Extract results and analyze field completeness
3. Generate statistics:
   - Total notes extracted
   - Notes with full content (>100 chars)
   - Notes with placeholders (needs-recrawl tag)
   - Notes with images (and average image count)
   - Notes with videos
   - Notes with author info
   - Notes with engagement metrics

If MCP is unavailable, perform architecture review instead:

- Verify all 6 core field extractors are implemented
- Confirm quality filtering is integrated
- Check session refresh logic exists
- Document expected behavior based on code review

Create verification output showing:

```
=== Xiaohongshu Crawler Verification ===
Total notes: X
Full content: X (Y%)
Placeholders: X (Y%)
With images: X (Y%)
With videos: X (Y%)
With author: X (Y%)
With engagement: X (Y%)
```

  </action>
  <verify>
```bash
# TypeScript compiles
cd apps/ai-service && pnpm exec tsc --noEmit && echo "TypeScript OK"

# Code review verification

echo "=== Code Review Checks ==="
echo "1. Full content extraction:"
grep -c "note.desc\|note.title" apps/ai-service/src/lib/crawlers/xiaohongshu.ts

echo "2. Image extraction with dimensions:"
grep -c "image_list\|width.*height\|height.*width" apps/ai-service/src/lib/crawlers/xiaohongshu.ts

echo "3. Video extraction:"
grep -c "h264\|h265\|video.\*stream\|videoUrls" apps/ai-service/src/lib/crawlers/xiaohongshu.ts

echo "4. Author extraction:"
grep -c "nickname\|avatar\|authorName\|authorAvatar" apps/ai-service/src/lib/crawlers/xiaohongshu.ts

echo "5. Engagement metrics:"
grep -c "liked_count\|collected_count\|comment_count\|likesCount\|savesCount\|commentsCount" apps/ai-service/src/lib/crawlers/xiaohongshu.ts

echo "6. Quality filtering:"
grep -c "detectPlaceholderContent\|isContentQualityAcceptable\|calculateXhsQualityScore" apps/ai-service/src/lib/crawlers/xiaohongshu.ts

```
  </verify>
  <done>
- TypeScript compiles without errors
- Code review confirms all 6 core field extractors are present
- Quality filtering is integrated into result conversion
- Session refresh logic exists for mid-crawl expiry handling
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Enhanced Xiaohongshu crawler with:
- Full content extraction from API responses
- Image URLs with dimensions from image_list
- Video URLs (H264/H265) with capture timestamp
- Author name and avatar from user object
- Engagement metrics (likes, saves, comments) from interact_info
- Content quality filtering (100+ char minimum)
- Placeholder detection and flagging
- Session auto-refresh on expiry
- Detail page navigation for video notes
  </what-built>
  <how-to-verify>
1. If MCP/Chrome available:
   - Ensure logged into Xiaohongshu (run login-helper if needed)
   - Run crawler test: `cd apps/ai-service && pnpm exec tsx -e "import { crawlXiaohongshu } from './src/lib/crawlers/index.js'; crawlXiaohongshu('北京', { maxPages: 1 }).then(r => console.log(JSON.stringify(r.slice(0,2), null, 2)))"`
   - Verify output shows: title, content (>100 chars), imageUrls with dimensions, author info, engagement metrics
   - Check console logs for "Extraction stats: X full content, Y placeholders"

2. If MCP unavailable (architecture review):
   - Confirm all XHS requirements met via code inspection
   - Verify TypeScript compiles: `cd apps/ai-service && pnpm exec tsc --noEmit`
   - Check extraction patterns match API response structure

Expected: Notes with full content, images, optional videos, author info, and engagement metrics extracted correctly.
  </how-to-verify>
  <resume-signal>Type "approved" if verification passes (with stats), or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 7 requirements verification:

| Requirement | Status | Evidence |
|-------------|--------|----------|
| XHS-01: Full note text | ✓/✗ | Content >100 chars extracted |
| XHS-02: High-res images | ✓/✗ | image_list with dimensions |
| XHS-03: Video URLs | ✓/✗ | H264/H265 stream extraction |
| XHS-04: Author info | ✓/✗ | nickname + avatar from user |
| XHS-05: Publish time | ✓/✗ | (Note: XHS API may not include this) |
| XHS-06: Engagement | ✓/✗ | liked/collected/comment counts |
| XHS-07: Login support | ✓/✗ | Session module integration |
| XHS-08: Data completeness | ✓/✗ | Quality filtering active |
</verification>

<success_criteria>
1. TypeScript compiles without errors
2. All 6 core field extractors present in code
3. Quality filtering integrated (100+ char minimum, placeholder detection)
4. Session refresh logic exists
5. Human verification confirms correct behavior (or architecture review if MCP unavailable)
</success_criteria>

<output>
After completion, create `.planning/phases/07-xiaohongshu/07-03-SUMMARY.md`
</output>
```
