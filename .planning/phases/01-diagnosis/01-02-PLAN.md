---
phase: 01-diagnosis
plan: 02
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - apps/ai-service/src/diagnose-crawlers.ts
  - .planning/phases/01-diagnosis/diagnostics/ctrip-diagnosis.md
  - .planning/phases/01-diagnosis/diagnostics/qunar-diagnosis.md
autonomous: true

must_haves:
  truths:
    - 'Ctrip diagnostic capture runs and produces diagnosis report'
    - 'Qunar diagnostic capture runs and produces diagnosis report'
    - 'Root cause identified for each platform (acquisition vs parsing)'
  artifacts:
    - path: 'apps/ai-service/src/diagnose-crawlers.ts'
      provides: 'Diagnostic entry point script'
      exports: ['runDiagnostic']
    - path: '.planning/phases/01-diagnosis/diagnostics/ctrip-diagnosis.md'
      provides: 'Ctrip diagnostic findings'
      contains: 'Failure Category'
    - path: '.planning/phases/01-diagnosis/diagnostics/qunar-diagnosis.md'
      provides: 'Qunar diagnostic findings'
      contains: 'Failure Category'
  key_links:
    - from: 'apps/ai-service/src/diagnose-crawlers.ts'
      to: 'apps/ai-service/src/lib/crawlers/diagnostics/index.ts'
      via: 'import captureForDiagnosis, generateDiagnosticReport'
      pattern: 'import.*from.*diagnostics'
---

<objective>
Run diagnostic capture on Ctrip and Qunar platforms to identify root causes of data incompleteness.

Purpose: These two platforms already navigate to detail pages and are relatively more complete - diagnose them first to establish baseline patterns.

Output: Diagnostic reports for Ctrip and Qunar with failure categories and recommended actions.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-diagnosis/01-RESEARCH.md
@.planning/phases/01-diagnosis/01-01-SUMMARY.md
@apps/ai-service/src/lib/crawlers/ctrip.ts
@apps/ai-service/src/lib/crawlers/qunar.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create diagnostic entry point script</name>
  <files>apps/ai-service/src/diagnose-crawlers.ts</files>
  <action>
Create diagnose-crawlers.ts with:

1. Import from diagnostics/index.ts: captureForDiagnosis, generateDiagnosticReport
2. Import from mcp-client.ts: initMCP, disconnect

3. `runDiagnostic(platform: string, url: string)`:
   - Initialize MCP (isolated mode for diagnostics)
   - Call captureForDiagnosis(platform, url)
   - Generate report with generateDiagnosticReport
   - Print report to console
   - Disconnect MCP
   - Return capture object

4. CLI support:
   - Parse command line args: `--platform` and `--url`
   - If args provided, run diagnostic for that platform/url
   - Print usage if missing args

5. Export runDiagnostic for programmatic use

Example usage: `npx tsx apps/ai-service/src/diagnose-crawlers.ts --platform ctrip --url "https://..."`
</action>
<verify>
1. Run: `npx tsc --noEmit apps/ai-service/src/diagnose-crawlers.ts`
   Expect: No TypeScript errors
2. Run a test diagnostic to verify script works:
   `npx tsx apps/ai-service/src/diagnose-crawlers.ts --platform test --url "https://example.com" 2>&1 | head -20`
   Expect: Script runs without crashing (output shows diagnostic attempt or usage message)
</verify>
<done>
diagnose-crawlers.ts provides CLI and programmatic interface for running diagnostics on any platform.
</done>
</task>

<task type="auto">
  <name>Task 2: Run diagnostics on Ctrip and Qunar</name>
  <files>
.planning/phases/01-diagnosis/diagnostics/ctrip-diagnosis.md
.planning/phases/01-diagnosis/diagnostics/qunar-diagnosis.md
  </files>
  <action>
For each platform (Ctrip, Qunar):

1. Find a sample travel guide URL:
   - Ctrip: Navigate to a list page (e.g., Beijing guides), extract a detail page URL
   - Qunar: Navigate to a list page, extract a detail page URL

2. Run diagnostic:

   ```bash
   npx tsx apps/ai-service/src/diagnose-crawlers.ts --platform ctrip --url "<detail-url>"
   npx tsx apps/ai-service/src/diagnose-crawlers.ts --platform qunar --url "<detail-url>"
   ```

3. Run existing crawler for comparison:
   - Import and run the existing ctrip/qunar crawler on the same URL
   - Capture what data the current crawler returns
   - Note any parsing errors or empty fields

4. Analyze the diagnostic output vs existing crawler:
   - What is the failure category?
   - What's in the snapshot (preview)?
   - Are there anti-bot indicators?
   - How many network requests captured?
   - What's the timing data?
   - Compare: What data does diagnostic capture vs what current crawler extracts?

5. Create diagnosis report in `.planning/phases/01-diagnosis/diagnostics/` with:
   - Platform name
   - Test URL used
   - Failure category
   - Root cause analysis (acquisition vs parsing)
   - Anti-bot observations
   - Recommended fix approach
   - Any platform-specific quirks discovered
   - Existing crawler output comparison (what it currently extracts)

If diagnostic script fails, document the error and analyze manually by reading crawler code.
</action>
<verify>
Check files exist:

- .planning/phases/01-diagnosis/diagnostics/ctrip-diagnosis.md
- .planning/phases/01-diagnosis/diagnostics/qunar-diagnosis.md

Each file should contain:

- Failure Category section
- Root Cause Analysis section
- Recommended Fix section
  </verify>
  <done>
  Ctrip and Qunar diagnostic reports created with clear failure categories and recommended fixes.
  </done>
  </task>

</tasks>

<verification>
1. diagnose-crawlers.ts compiles and runs without errors
2. Both diagnosis report files exist with required sections
3. Each report clearly states whether issue is acquisition or parsing
</verification>

<success_criteria>

- Diagnostic script runs successfully on both platforms
- Root cause identified for each platform (acquisition vs parsing stage)
- Recommended fix approach documented for each platform
  </success_criteria>

<output>
After completion, create `.planning/phases/01-diagnosis/01-02-SUMMARY.md`
</output>
