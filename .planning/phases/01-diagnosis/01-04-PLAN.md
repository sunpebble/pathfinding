---
phase: 01-diagnosis
plan: 04
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - .planning/phases/01-diagnosis/diagnostics/xiaohongshu-diagnosis.md
autonomous: true

must_haves:
  truths:
    - 'Xiaohongshu diagnostic capture runs with logged-out session (logged-in if session available)'
    - 'API endpoints identified and response structure analyzed'
    - 'Video URL extraction feasibility documented'
    - 'Login requirements and anti-bot mechanisms documented'
  artifacts:
    - path: '.planning/phases/01-diagnosis/diagnostics/xiaohongshu-diagnosis.md'
      provides: 'Xiaohongshu diagnostic findings with API analysis'
      contains: 'API Endpoints'
  key_links:
    - from: 'apps/ai-service/src/diagnose-crawlers.ts'
      to: 'apps/ai-service/src/lib/crawlers/diagnostics/index.ts'
      via: 'import captureForDiagnosis'
      pattern: 'import.*from.*diagnostics'
---

<objective>
Run diagnostic capture on Xiaohongshu platform to identify root causes of data incompleteness.

Purpose: Xiaohongshu is the most complex platform with API-based data delivery, strong anti-bot protection, and video content. This requires special analysis of network requests.

Output: Comprehensive diagnostic report for Xiaohongshu covering API endpoints, login requirements, video extraction, and anti-bot mechanisms.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-diagnosis/01-RESEARCH.md
@.planning/phases/01-diagnosis/01-01-SUMMARY.md
@apps/ai-service/src/lib/crawlers/xiaohongshu.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze Xiaohongshu current implementation</name>
  <files>.planning/phases/01-diagnosis/diagnostics/xiaohongshu-diagnosis.md</files>
  <action>
1. Read xiaohongshu.ts crawler thoroughly:
   - What URL patterns does it navigate to?
   - How does it extract data (snapshot parsing vs API interception)?
   - Does it handle video content?
   - What login/session handling exists?

2. Run diagnostic WITHOUT login (isolated session):

   ```bash
   npx tsx apps/ai-service/src/diagnose-crawlers.ts --platform xiaohongshu --url "https://www.xiaohongshu.com/explore"
   ```

   - Capture snapshot and network requests
   - Note any anti-bot indicators

3. Run existing crawler for comparison:
   - Import and run the existing xiaohongshu crawler on the same URL
   - Note what data the current crawler extracts vs what diagnostic captures

4. Test with login (if session available):
   - Check if login-helper has saved session for xiaohongshu (optional - session may not exist)
   - If session exists, run diagnostic with persistent browser context
   - Compare logged-in vs logged-out results
   - If no session available, note that login testing was skipped and recommend manual login setup

5. Analyze network requests specifically:
   - Look for API endpoints: /api/sns/web/v1/feed, /api/sns/web/v1/homefeed
   - Capture sample API response structure
   - Check if responses are encrypted/signed

6. Test a specific note URL:
   - Navigate to a specific note: https://www.xiaohongshu.com/explore/[note-id]
   - Analyze what data is in snapshot vs API

7. Document in xiaohongshu-diagnosis.md:
   - Current crawler implementation analysis
   - Logged-out vs logged-in comparison (if login session was available)
   - API endpoints discovered (URL patterns, response structure)
   - Anti-bot mechanisms observed
   - Video URL extraction feasibility
   - CDN URL expiration observations
   - Recommended fix approach

Use detailed sections for API analysis since this is the most complex platform.
</action>
<verify>
Check file exists: .planning/phases/01-diagnosis/diagnostics/xiaohongshu-diagnosis.md
File should contain:

- Current Implementation Analysis section
- API Endpoints section
- Login Requirements section
- Video Extraction section
- Anti-bot Mechanisms section
- Recommended Fix section
  </verify>
  <done>
  Comprehensive Xiaohongshu diagnostic report with API analysis, login requirements, video feasibility, and anti-bot documentation.
  </done>
  </task>

</tasks>

<verification>
1. Diagnosis report file exists with all required sections
2. API endpoints clearly documented with response structure
3. Login vs logged-out behavior compared (if session was available, otherwise noted as skipped)
4. Video extraction feasibility clearly stated
5. Anti-bot mechanisms documented
</verification>

<success_criteria>

- API endpoints identified and response structure analyzed
- Login requirements clearly documented (what content requires login)
- Video URL extraction feasibility determined
- CDN URL expiration timeline documented
- Specific recommended fixes provided
  </success_criteria>

<output>
After completion, create `.planning/phases/01-diagnosis/01-04-SUMMARY.md`
</output>
