---
phase: 06-tongcheng
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ai-service/src/lib/crawlers/accessibility-parser.ts
autonomous: true

must_haves:
  truths:
    - 'extractTongchengStats() parses Chinese numbers like 2.7千 → 2700'
    - 'extractTongchengAuthor() extracts author name and avatar URL'
    - 'transformToHighResTc() removes size constraints from Tongcheng CDN URLs'
  artifacts:
    - path: 'apps/ai-service/src/lib/crawlers/accessibility-parser.ts'
      provides: 'Tongcheng-specific extraction utilities'
      exports:
        [
          'extractTongchengStats',
          'extractTongchengAuthor',
          'transformToHighResTc',
        ]
  key_links:
    - from: 'extractTongchengStats'
      to: 'parseChineseNumber'
      via: 'internal function call'
      pattern: "parseChineseNumber\\("
---

<objective>
Add Tongcheng-specific parsing utilities following the established Mafengwo/Qunar/Ctrip pattern.

Purpose: These extractors will be used by the restructured Tongcheng crawler to parse full content from detail pages. Following the proven pattern from Phases 3-5.

Output: Three new exported functions in accessibility-parser.ts: extractTongchengStats(), extractTongchengAuthor(), transformToHighResTc()
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tongcheng/06-RESEARCH.md

@apps/ai-service/src/lib/crawlers/accessibility-parser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add extractTongchengStats function</name>
  <files>apps/ai-service/src/lib/crawlers/accessibility-parser.ts</files>
  <action>
Add extractTongchengStats() following the exact pattern of extractMafengwoStats() (lines 906-1004).

The function should:

1. Accept content: string parameter
2. Return { views?: number; likes?: number; saves?: number; comments?: number }
3. Use parseChineseNumber() internally for all numeric parsing (handles "2.7千", "11.5万")
4. Support both inline patterns ("浏览量2.7千") and accessibility tree patterns (StaticText "浏览量" followed by StaticText "2700")

Pattern details from research:

- Views: "浏览量 1234", "1234 次浏览", "阅读 2.5万"
- Likes: "点赞 123", "顶 45", "123 赞"
- Saves: "收藏 85", "85 人收藏"
- Comments: "评论 30", "30 条评论"

Add comprehensive JSDoc comment explaining the function purpose and patterns handled.
</action>
<verify>
TypeScript compiles without errors:

```bash
cd apps/ai-service && npx tsc --noEmit
```

Function is exported (grep for "export function extractTongchengStats")
</verify>
<done>
extractTongchengStats() exists in accessibility-parser.ts, is exported, uses parseChineseNumber internally, handles both inline and accessibility tree formats.
</done>
</task>

<task type="auto">
  <name>Task 2: Add extractTongchengAuthor and transformToHighResTc functions</name>
  <files>apps/ai-service/src/lib/crawlers/accessibility-parser.ts</files>
  <action>
Add extractTongchengAuthor() following the pattern of extractMafengwoAuthor() (lines 1018-1080).

extractTongchengAuthor() should:

1. Accept content: string parameter
2. Return { name?: string; avatar?: string }
3. Try patterns in priority order:
   - Pattern 1: Look for labeled author (作者：, 发布者：, by)
   - Pattern 2: Look for name near ly.com/user profile links
   - Pattern 3: Fallback to generic extractAuthor()
4. Extract avatar URL (best effort) looking for:
   - Tongcheng CDN patterns: ly.com, tcimg containing avatar/head/user
   - Generic avatar patterns

Add isValidTongchengAuthor() helper function (following isValidMafengwoAuthorName pattern):

- Filter names < 2 or > 30 chars
- Filter URLs (contains http)
- Filter common UI text: 分享, 收藏, 关注, 点赞, 评论, 返回, 首页, 登录, 注册, 同程, 攻略, 旅游

Add transformToHighResTc() following the pattern of transformToHighResMfw() (lines 1116-1154).

transformToHighResTc() should:

1. Accept url: string parameter
2. Return transformed URL (or original if not Tongcheng CDN)
3. Check for Tongcheng CDN domains: ly.com, tcimg, tongcheng
4. Apply transformations:
   - Remove thumbnail suffixes: \_small, \_thumb, \_s
   - Remove size constraints in path: /180x180/
   - Remove/modify query params: delete w, h; set q=100
5. Return original URL if not from Tongcheng CDN or if parsing fails

Add comprehensive JSDoc comments for all three functions.
</action>
<verify>
TypeScript compiles without errors:

```bash
cd apps/ai-service && npx tsc --noEmit
```

All functions are exported:

```bash
grep -E "export function (extractTongchengAuthor|transformToHighResTc|isValidTongchengAuthor)" apps/ai-service/src/lib/crawlers/accessibility-parser.ts
```

  </verify>
  <done>
extractTongchengAuthor() and transformToHighResTc() exist in accessibility-parser.ts, are exported, follow established patterns. isValidTongchengAuthor() helper exists.
  </done>
</task>

</tasks>

<verification>
All Tongcheng parsing utilities compile and are exported:

```bash
cd apps/ai-service && npx tsc --noEmit
grep -E "export function (extractTongchengStats|extractTongchengAuthor|transformToHighResTc)" src/lib/crawlers/accessibility-parser.ts
```

Expected: 3 exported functions matching the grep pattern.
</verification>

<success_criteria>

1. extractTongchengStats() parses Chinese numbers correctly (uses parseChineseNumber)
2. extractTongchengAuthor() extracts author name and avatar with Tongcheng-specific patterns
3. transformToHighResTc() transforms Tongcheng CDN URLs to high-resolution versions
4. All functions follow established Mafengwo/Qunar/Ctrip patterns
5. TypeScript compiles without errors
   </success_criteria>

<output>
After completion, create `.planning/phases/06-tongcheng/06-01-SUMMARY.md`
</output>
