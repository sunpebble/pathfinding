---
phase: 02-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/crawlers/ctrip.ts
  - src/lib/crawlers/mafengwo.ts
  - src/lib/crawlers/qunar.ts
  - src/lib/crawlers/tongcheng.ts
  - src/lib/crawlers/xiaohongshu.ts
autonomous: true

must_haves:
  truths:
    - 'Crawlers use waitForContentStable() instead of fixed sleep() after navigation'
    - 'Fixed sleep() is retained only for rate limiting between requests'
    - 'Content loading is detected dynamically, not by arbitrary delays'
  artifacts:
    - path: 'src/lib/crawlers/ctrip.ts'
      provides: 'Ctrip crawler with smart wait'
      contains: 'waitForContentStable'
    - path: 'src/lib/crawlers/mafengwo.ts'
      provides: 'Mafengwo crawler with smart wait'
      contains: 'waitForContentStable'
    - path: 'src/lib/crawlers/qunar.ts'
      provides: 'Qunar crawler with smart wait'
      contains: 'waitForContentStable'
    - path: 'src/lib/crawlers/tongcheng.ts'
      provides: 'Tongcheng crawler with smart wait'
      contains: 'waitForContentStable'
    - path: 'src/lib/crawlers/xiaohongshu.ts'
      provides: 'Xiaohongshu crawler with smart wait'
      contains: 'waitForContentStable'
  key_links:
    - from: 'src/lib/crawlers/ctrip.ts'
      to: 'src/lib/crawlers/diagnostics/index.ts'
      via: 'imports waitForContentStable'
      pattern: 'import.*waitForContentStable.*from.*diagnostics'
    - from: 'src/lib/crawlers/xiaohongshu.ts'
      to: 'src/lib/crawlers/diagnostics/index.ts'
      via: 'imports waitForContentStable'
      pattern: 'import.*waitForContentStable.*from.*diagnostics'
---

<objective>
Replace fixed sleep() delays with smart waitForContentStable() in all platform crawlers.

Purpose: Eliminate arbitrary delays that either waste time (content loaded faster) or fail to wait long enough (content still loading). Smart wait detects when page content has stabilized, improving reliability and speed.

Output: Updated crawlers that use `waitForContentStable()` after navigation, while keeping `sleep()` for rate limiting between requests.
</objective>

<execution_context>
@/Users/shikun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shikun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/crawlers/diagnostics/capture.ts
@src/lib/crawlers/diagnostics/index.ts
@src/lib/crawlers/mcp-client.ts

Key insights:

- `waitForContentStable()` already exists in diagnostics/capture.ts and is exported from diagnostics/index.ts
- Current crawlers use fixed sleep(2000-3000) after navigation
- sleep() for rate limiting (e.g., `sleep(1000 / rateLimit)`) should be kept
- scrollToLoadContent() has internal sleep() for scroll timing - leave as is
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Update Ctrip crawler</name>
  <files>src/lib/crawlers/ctrip.ts</files>
  <action>
Update ctrip.ts to use waitForContentStable() instead of fixed sleep() after navigation.

Changes:

1. Add import: `import { waitForContentStable } from './diagnostics/index.js';`
2. In `fetchListPage()`:
   - Replace `await sleep(2000);` after navigateTo with `await waitForContentStable();`
   - Keep `await sleep(1000);` after scrollToLoadContent (scroll stabilization)
3. In `fetchGuideDetail()`:
   - Replace `await sleep(2000);` after navigateTo with `await waitForContentStable();`
   - Keep `await sleep(1000);` after scrollToLoadContent
4. Keep `await sleep(1000 / (options.rateLimit || 0.5));` for rate limiting (line 62)

Pattern to apply:

```typescript
// BEFORE
await navigateTo(url, { timeout: 30000 });
await sleep(2000);

// AFTER
await navigateTo(url, { timeout: 30000 });
await waitForContentStable();
```

Do NOT modify:

- Rate limiting sleep() between guide fetches
- scrollToLoadContent() internal timing
  </action>
  <verify>

1. `grep -n "waitForContentStable" src/lib/crawlers/ctrip.ts` shows import and usage
2. `grep -n "sleep(2000)" src/lib/crawlers/ctrip.ts` returns empty (no more fixed post-nav sleeps)
3. `grep -n "rateLimit" src/lib/crawlers/ctrip.ts` still shows rate limiting sleep
4. `npx tsc --noEmit` compiles without errors
   </verify>
   <done>
   ctrip.ts uses waitForContentStable() after both list and detail navigation, retains rate limiting sleep.
   </done>
   </task>

<task type="auto">
  <name>Task 2: Update remaining crawlers</name>
  <files>
src/lib/crawlers/mafengwo.ts
src/lib/crawlers/qunar.ts
src/lib/crawlers/tongcheng.ts
src/lib/crawlers/xiaohongshu.ts
  </files>
  <action>
Apply the same pattern to all remaining crawlers:

**For each crawler file:**

1. Add import at top:

```typescript
import { waitForContentStable } from './diagnostics/index.js';
```

2. Replace fixed post-navigation sleep() with waitForContentStable():

**mafengwo.ts:**

- Replace `await sleep(3000);` after navigateTo (around line 90) with `await waitForContentStable();`
- Keep `await sleep(2000);` after scrollToLoadContent

**qunar.ts:**

- Replace `await sleep(2000);` after navigateTo in fetchListPage (around line 94) with `await waitForContentStable();`
- Replace `await sleep(2000);` after navigateTo in fetchGuideDetail (around line 125) with `await waitForContentStable();`
- Keep `await sleep(1000);` after scrollToLoadContent
- Keep `await sleep(1000 / (options.rateLimit || 0.5));` for rate limiting

**tongcheng.ts:**

- Replace `await sleep(3000);` after navigateTo (around line 77) with `await waitForContentStable();`
- Keep `await sleep(2000);` after scrollToLoadContent

**xiaohongshu.ts:**

- Replace `await sleep(3000);` after navigateTo (around line 120) with `await waitForContentStable();`
- Keep `await sleep(2000);` after scrollToLoadContent

Pattern consistency:

- Import from `./diagnostics/index.js` (relative path from crawler files)
- waitForContentStable() uses default params (10s max, 500ms window)
- Keep all scroll-related and rate-limiting sleeps unchanged
  </action>
  <verify>
  For each crawler (mafengwo, qunar, tongcheng, xiaohongshu):

1. `grep -n "waitForContentStable" src/lib/crawlers/[name].ts` shows import and at least one usage
2. Fixed post-navigation sleep(3000) or sleep(2000) removed
3. `npx tsc --noEmit` compiles all crawlers without errors

Run full check:

```bash
grep -l "waitForContentStable" src/lib/crawlers/*.ts | wc -l
# Should output: 5 (all 5 platform crawlers)
```

  </verify>
  <done>
All 5 crawlers (ctrip, mafengwo, qunar, tongcheng, xiaohongshu) import and use waitForContentStable() after navigation.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. All crawlers import waitForContentStable:

```bash
grep -l "import.*waitForContentStable" src/lib/crawlers/*.ts
# Should show: ctrip.ts, mafengwo.ts, qunar.ts, tongcheng.ts, xiaohongshu.ts
```

2. No fixed post-navigation sleeps remain:

```bash
# This should return empty or only show rate-limiting/scroll sleeps
grep -n "await sleep(2000)" src/lib/crawlers/*.ts
grep -n "await sleep(3000)" src/lib/crawlers/*.ts
```

3. Rate limiting sleeps still present:

```bash
grep -n "rateLimit" src/lib/crawlers/*.ts
# Should show ctrip.ts and qunar.ts
```

4. TypeScript compiles:

```bash
npx tsc --noEmit
```

</verification>

<success_criteria>

- All 5 platform crawlers updated with waitForContentStable() import
- Fixed sleep() after navigation replaced with smart wait
- Rate limiting sleep() preserved
- Scroll-related sleep() preserved
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure/02-02-SUMMARY.md`
</output>
