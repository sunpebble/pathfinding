---
phase: 05-mafengwo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ai-service/src/lib/crawlers/accessibility-parser.ts
autonomous: true

must_haves:
  truths:
    - 'Mafengwo stats extraction handles Chinese numbers (2.7千 → 2700)'
    - 'Mafengwo author extraction uses platform-specific patterns'
    - 'Mafengwo image URLs are transformed to high-resolution versions'
    - 'Captcha detection identifies verification pages'
  artifacts:
    - path: 'apps/ai-service/src/lib/crawlers/accessibility-parser.ts'
      provides: 'Mafengwo-specific parsing utilities'
      exports:
        [
          'extractMafengwoAuthor',
          'extractMafengwoStats',
          'transformToHighResMfw',
        ]
  key_links:
    - from: 'accessibility-parser.ts'
      to: 'parseChineseNumber'
      via: 'internal function call'
      pattern: 'parseChineseNumber\\('
---

<objective>
Add Mafengwo-specific parsing utilities to accessibility-parser.ts.

Purpose: Provide platform-specific extractors for author, stats, and image URL transformation that understand Mafengwo's page structure and CDN patterns. These utilities will be used by the restructured crawler in Plan 02.

Output: Three new exported functions (extractMafengwoAuthor, extractMafengwoStats, transformToHighResMfw) following the established patterns from Ctrip/Qunar phases.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mafengwo/05-RESEARCH.md

Key source files:
@apps/ai-service/src/lib/crawlers/accessibility-parser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Mafengwo stats extraction utility</name>
  <files>apps/ai-service/src/lib/crawlers/accessibility-parser.ts</files>
  <action>
Add a new exported function following the extractQunarStats pattern:

**extractMafengwoStats(content: string): { views?: number; likes?: number; saves?: number; comments?: number }**

Implementation:

1. **Views extraction patterns:**
   - Inline: /(?:浏览量?|阅读量?)[：:\s]\*([\d.]+[千万kw]?)/i
   - Alternative: /([\d.]+[千万kw]?)\s\*(?:次浏览|阅读)/i
   - Accessibility tree: /StaticText\s+"(?:浏览量?|阅读量?)"[\s\S]{0,100}?StaticText\s+"([\d.]+[千万kw]?)"/i

2. **Likes extraction patterns:**
   - Inline: /(?:点赞|顶)[：:\s]\*([\d.]+[千万kw]?)/i
   - Alternative: /([\d.]+[千万kw]?)\s\*(?:点赞|个赞|赞)/i
   - Accessibility tree pattern with same fallback

3. **Saves extraction patterns:**
   - Inline: /收藏[：:\s]\*([\d.]+[千万kw]?)/i
   - Alternative: /([\d.]+[千万kw]?)\s\*(?:人收藏|收藏)/i
   - Accessibility tree pattern with same fallback

4. **Comments extraction patterns:**
   - Inline: /评论[：:\s]\*([\d.]+[千万kw]?)/i
   - Alternative: /([\d.]+[千万kw]?)\s\*条评论/i
   - Accessibility tree pattern with same fallback

Use parseChineseNumber() internally for all numeric parsing (already exists in the file).

Add JSDoc comment explaining the function's purpose.
</action>
<verify>
Compile check: npx tsc --noEmit -p apps/ai-service/tsconfig.json
Function should be exported and callable.
</verify>
<done>extractMafengwoStats function exports and parses Chinese engagement metrics using established parseChineseNumber utility.</done>
</task>

<task type="auto">
  <name>Task 2: Add Mafengwo author extraction and high-res image utilities</name>
  <files>apps/ai-service/src/lib/crawlers/accessibility-parser.ts</files>
  <action>
Add two new exported functions following the established patterns:

**1. extractMafengwoAuthor(content: string): { name?: string; avatar?: string }**

Implementation:

1. **Author name extraction patterns (in priority order):**
   - Pattern 1: /(?:作者|发布者|by)[：:\s]+"?([^"\n\]]{2,30})"?/i (labeled author)
   - Pattern 2: /StaticText\s+"([^"]{2,20})"\s\*[\s\S]{0,100}?u\.mafengwo\.cn\/\d+/ (name near profile link)
   - Pattern 3: Fallback to existing extractAuthor(content) function

2. **Author name validation (add isValidMafengwoAuthor helper):**
   - Length 2-30 characters
   - No 'http' in name
   - Not in blacklist: 分享|收藏|关注|点赞|评论|返回|首页|登录|注册|马蜂窝|攻略

3. **Avatar extraction (best effort):**
   - Pattern 1: /(https?:\/\/[^\s"']_mafengwo[^\s"']_(?:avatar|head|user)[^\s"']\*\.(?:jpg|jpeg|png|webp))/i
   - Pattern 2: /(?:avatar|头像|author)[^"]\*?(https?:\/\/[^\s"']+\.(?:jpg|jpeg|png|webp))/i
   - Filter out URLs containing 'placeholder', 'icon', 'default'

**2. transformToHighResMfw(url: string): string**

Implementation:

1. Check if URL is from Mafengwo CDN:
   - url.includes('mafengwo.net') || url.includes('mafengwo.cn')

2. If not Mafengwo CDN, return url unchanged

3. Apply transformations:
   - Remove thumbnail suffix: url.replace(/\.\d+\.[wh]\.(?:jpg|jpeg|png|webp)$/i, '.jpg')
   - Try URL parsing to remove w/h query params and set q=100

Add JSDoc comments explaining each function's purpose.
</action>
<verify>

1. Compile check: npx tsc --noEmit -p apps/ai-service/tsconfig.json
2. Verify all three new functions are exported
   </verify>
   <done>extractMafengwoAuthor and transformToHighResMfw functions export and provide Mafengwo-specific author/avatar extraction and CDN URL transformation.</done>
   </task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit -p apps/ai-service/tsconfig.json`
2. All new functions are exported from accessibility-parser.ts
3. No breaking changes to existing extractors used by other crawlers
</verification>

<success_criteria>

- extractMafengwoStats exports and uses parseChineseNumber internally
- extractMafengwoAuthor exports with name and avatar extraction
- transformToHighResMfw exports and handles Mafengwo CDN patterns
- All existing exports remain functional (backward compatible)
- TypeScript compiles successfully
  </success_criteria>

<output>
After completion, create `.planning/phases/05-mafengwo/05-01-SUMMARY.md`
</output>
